<nav
  class="navbar navbar-expand-lg fixed-top"
  style="background-color: #343a40"
>
  <div class="container-fluid">
    <button class="btn btn-light" id="sidebarToggle">
      <span class="navbar-toggler-icon"></span>
    </button>
    <h1 class="navbar-brand mb-0 h1">Admin Dashboard</h1>
    <div class="ml-auto d-flex align-items-center">
      <button class="btn btn-light position-relative">
        <i class="bi bi-chat-dots" style="font-size: 1.5rem; color: black"></i>
        <span class="badge badge-danger position-absolute rounded-circle"
          >3</span
        >
      </button>
      <button class="btn btn-light ml-3" onclick="confirmLogout()">
        <i class="bi bi-box-arrow-right"></i> Logout
      </button>
    </div>
  </div>
</nav>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<script>
  function confirmLogout() {
    Swal.fire({
      title: "Are you sure?",
      icon: "warning",
      showCancelButton: true,
      confirmButtonColor: "#3085d6",
      cancelButtonColor: "#d33",
      confirmButtonText: "Yes",
    }).then((result) => {
      if (result.isConfirmed) {
        window.location.href = "/admin/adminLogout";
      }
    });
  }
</script>
